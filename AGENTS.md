# Agent operating contract

Canonical target: `caliofmarian-ai/DROPi-Home-Affiliate`, PRIVATE. Owner: Marian. Do not modify other repositories to work around access or creation limitations.

1. Read README, `docs/HANDOFF.md`, `docs/LAUNCH-CHECKLIST.md`, and current data before edits. Check the live repository and branch SHA before any remote write. Do not overwrite unknown existing content.
2. Keep source, content, tests and operational documentation together. Asset tools may assist later; they are not an alternative authority.
3. Owner reports are Romanian; repository documentation and public content are English.
4. Never invent programme acceptance, tracking IDs, payments, prices, ratings, stock, deliveries, licensing, personal tests or owner approvals. Test fixtures are synthetic, never operational evidence.
5. Do not activate spending, ad buying, subscriptions, domains, hosting, recurring jobs, public publication or monetisation without the necessary explicit authorisation. Current authorised additional spend is zero.
6. Read and verify the exact current external source before changing `checkedAt`. Never advance dates merely to make a check pass.
7. Real credentials, private financial reports and identity documents never belong in Git. The public website must not serve project source, approval evidence, `.env`, local statement files or the owner workspace.
8. Maintain the no-dependency core unless a dependency has a concrete need and documented maintenance/licensing review. Never add an OpenAI key or billable model call merely to run this MVP.
9. Run `npm run check`, `npm test`, `npm run build`, and the expected-HOLD release check. Separate SOURCE VERIFIED, LOCAL TESTED, CI VERIFIED, DEPLOYED and OWNER ACCEPTED.
10. Do not store hidden chain of thought. Store decisions, source references, concise rationale, outputs, test results and unresolved work.
